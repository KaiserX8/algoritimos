for (let i=0; i < 100; i++) {
    let nome =prompt('Digite o seu nome');
    let idade = prompt('Digite a sua idade');
    if(idade >= 0 && idade <= 150);
    let salario =prompt('Informe o seu salario mensal')
    if (let) {
        
    }
    let sexo =('Informe o seu sexo')
    if (let) {}
    let status =('Informe o seu Estado Civil')
    if (let) {}
}